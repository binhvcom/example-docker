apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    url: http://prometheus:9090
  - name: Graphite
    type: graphite
    access: proxy
    url: http://graphite:8080
    jsonData:
      graphiteVersion: '1.1'
